{
  "name": "backend",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "cd ../frontend && bun install && vite build --outDir=../backend/frontend/dist",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "encore.dev": "^1.49.3"
  },
  "devDependencies": {
    "@types/node": "^22.18.1",
    "typescript": "^5.9.2",
    "vite": "^5.4.19",
    "vitest": "^3.2.4"
  },
  "overrides": {
    "esbuild": "0.21.5"
  }
}
